[package]
name = "out-zero-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
homepage.workspace = true
description = "Design-by-contract precondition and postcondition macros for no_std, with a `checked` mode for always-on assertions."
readme = "README.md"
keywords = ["contract", "no-std", "precondition", "postcondition", "design-by-contract"]
categories = ["no-std", "development-tools", "rust-patterns"]
publish = false

[lib]
name = "out_zero_contract"

[features]
default = []
checked = []
# Enables the `formal` module: anchors and documentation for discharging
# `requires!`/`ensures!` contracts with Kani, Creusot, or Prusti. No external
# toolchain is pulled in by this feature.
formal = []

[lints]
workspace = true

