[package]
name = "example-refinement-vs-witness-vs-ghost"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
out-zero-formal = { path = "../../crates/out-zero-formal", features = ["refinement", "witness", "ghost"] }
