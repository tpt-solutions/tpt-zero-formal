[package]
name = "example-telos-transpile-target"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
out-zero-formal = { path = "../../crates/out-zero-formal", features = ["contract", "refinement", "bounded"] }
