[package]
name = "example-telos-transpile-target"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tpt-zero-formal = { path = "../../crates/tpt-zero-formal", features = ["contract", "refinement", "bounded"] }
